#!/usr/bin/env bash
# Displays the content of the file "/etc/passwd"
# Displays only 'username', 'user id', and 'home directory'

i=1
path="/etc/passwd"

while IFS=: read -r username _ uid _ _ home _;
do
    echo "$username:$uid:$home" | cut -d ':' -s -f 1,3,6
done < $path
